set -g default-terminal "tmux-256color"
set -ag terminal-overrides ",xterm-256color:RGB"
set-option -sg escape-time 10
set-option -g focus-events on
set -g mouse on
set -g history-limit 30000
set -g status-left ""
set -g status-right ""

# Start windows and panes index at 1, not 0.
set -g base-index 1
set -g renumber-windows on
setw -g pane-base-index 1

# Position
set -g status-justify absolute-centre
set -g status-position top
set -g status-style "bg=default"
set -g window-status-current-style "fg=blue bold" # Color active window-text blue

bind r source-file ~/.config/tmux/tmux.conf

# switch panes using Alt-arrow without prefix
bind -n M-Left select-pane -L
bind -n M-Right select-pane -R
bind -n M-Up select-pane -U
bind -n M-Down select-pane -D

# Set new panes to open in current directory
bind c new-window -c "#{pane_current_path}"
bind '"' split-window -c "#{pane_current_path}"
bind % split-window -h -c "#{pane_current_path}"

# # Catppuccin theme, use the manual installation 
# set -g @catppuccin_flavor 'mocha'
# set -g @catppuccin_window_current_number "" # don't show window number
# set -g @catppuccin_window_current_text " #W"
# set -g @catppuccin_window_number "" # don't show window number
# set -g @catppuccin_window_status_style "basic"
# set -g @catppuccin_window_text " #W"
# set -g @plugin 'tmux-plugins/tpm'
# 
# # Initialize TMUX plugin manager (keep this line at the very bottom of tmux.conf)
# run ~/.config/tmux/plugins/tpm/tpm
# run ~/.config/tmux/plugins/catppuccin/tmux/catppuccin.tmux

